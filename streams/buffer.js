//Representação do espaço na memoria do computador, são armazenados para fazer transações de dados
//Maneira mais performatica para escrever e ler na memoria

const buf = Buffer.from("gustavoo")

console.log(buf.toJSON())

